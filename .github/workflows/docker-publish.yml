name: Docker

on:
  push:
    branches: [ "master" ]
    # Numeric semver only - rejects typo'd tags like 'vfoo.bar.baz' which would
    # otherwise trigger a release build that emits no tags. The major version to
    # build is derived from the tag, e.g. v14.0.1 builds PostgreSQL 14 (see the
    # "Determine PostgreSQL major version" step).
    tags: [ 'v[0-9]+.[0-9]+.[0-9]+' ]
    # Skip the build for changes that can't affect the built image. paths-ignore
    # is OR'd, so a commit touching only these files is skipped; a commit that
    # also touches a build-relevant file still runs.
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'LICENSE'
      - 'docker-run.sh'
  pull_request:
    branches: [ "master" ]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'LICENSE'
      - 'docker-run.sh'
  # Manual build of an arbitrary major version (smoke test only, never publishes).
  workflow_dispatch:
    inputs:
      pg_version:
        description: "PostgreSQL major version to build (e.g. 14). Smoke test, no publish."
        required: false
        default: ""

# Cancel superseded in-progress runs on the same branch/PR; never cancel an
# in-flight tag (release) run.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  REGISTRY_DOCKERHUB: docker.io
  REGISTRY_GHCR: ghcr.io
  # github.repository as <account>/<repo>
  IMAGE_NAME: ${{ github.repository }}
  # login is only needed when we actually push (on tag pushes); gating it
  # here keeps PR/branch builds green, incl. Dependabot/fork PRs that have
  # no access to the registry secrets.
  IS_RELEASE: ${{ startsWith(github.ref, 'refs/tags/v') && github.event_name != 'pull_request' }}
  # The newest PostgreSQL major version: the single source of truth for which
  # release gets the `latest` tag. Bump this when releasing a new newest major.
  LATEST_MAJOR: "18"


# Third-party actions are SHA-pinned (with version comment) to defend
# against tag-move attacks on a compromised action repo. GitHub-owned
# actions/* are pinned by major - they're considered trusted. Dependabot
# (.github/dependabot.yml) bumps the pins (SHA + comment) automatically.
jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      # This is used to complete the identity challenge
      # with sigstore/fulcio when running outside of PRs.
      id-token: write
      # Lets the attest-build-provenance steps write SLSA provenance.
      attestations: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      # Determine which PostgreSQL major version to build:
      #   - tag push (v14.0.1)        -> the tag's major (14)
      #   - manual run with an input  -> that input
      #   - branch push / PR          -> LATEST_MAJOR (build the newest)
      # The result drives both the build-arg and the `latest` tag decision below,
      # so the git tag is the single source of truth for a release's version.
      - name: Determine PostgreSQL major version
        id: pgver
        env:
          REF_TYPE: ${{ github.ref_type }}
          REF_NAME: ${{ github.ref_name }}
          INPUT_VERSION: ${{ github.event.inputs.pg_version }}
        run: |
          if [ -n "$INPUT_VERSION" ]; then
            major="$INPUT_VERSION"
          elif [ "$REF_TYPE" = "tag" ]; then
            major="${REF_NAME#v}"   # v14.0.1 -> 14.0.1
            major="${major%%.*}"    # 14.0.1  -> 14
          else
            major="$LATEST_MAJOR"
          fi
          # Validate: must be digits only (guards against bad input / injection).
          case "$major" in
            ''|*[!0-9]*) echo "::error::invalid PostgreSQL major version: '$major'"; exit 1 ;;
          esac
          echo "major=${major}" >> "$GITHUB_OUTPUT"
          echo "Building PostgreSQL major version: ${major}"

      # Install the cosign tool except on PR
      # https://github.com/sigstore/cosign-installer
      # Pinned to v3 (cosign v2.x) so we get the current Sigstore TUF root.
      # The old v2.8.1 installer shipped cosign v1.13.1, which embeds a
      # rotated-out TUF key and fails Fulcio cert verification with
      # "updating to TUF remote mirror: invalid key".
      - name: Install cosign
        if: ${{ startsWith(github.ref, 'refs/tags/v') && github.event_name != 'pull_request' }}
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6  # v4.1.2

      - name: Set up QEMU
        uses: docker/setup-qemu-action@06116385d9baf250c9f4dcb4858b16962ea869c3  # v4.1.0

      - name: Setup Docker buildx
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd  # v4.0.0
        with:
          platforms: linux/amd64,linux/arm64

      # Login against Docker Hub
      # https://github.com/docker/login-action
      - name: Log into Docker Hub
        if: ${{ env.IS_RELEASE == 'true' }}
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121  # v4.1.0
        with:
          registry: ${{ env.REGISTRY_DOCKERHUB }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # Login against GitHub Container Registry
      - name: Log into GHCR
        if: ${{ env.IS_RELEASE == 'true' }}
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121  # v4.1.0
        with:
          registry: ${{ env.REGISTRY_GHCR }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Extract metadata (tags, labels) for Docker
      # https://github.com/docker/metadata-action
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf  # v6.0.0
        with:
          images: |
            ${{ env.REGISTRY_DOCKERHUB }}/${{ env.IMAGE_NAME }}
            ${{ env.REGISTRY_GHCR }}/${{ env.IMAGE_NAME }}
          # Never auto-assign `latest`; emit it explicitly below only for the
          # newest major. This keeps `latest` stable regardless of tag-push order.
          flavor: |
            latest=false
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{major}}
            type=raw,value=latest,enable=${{ github.ref_type == 'tag' && steps.pgver.outputs.major == env.LATEST_MAJOR }}
      # Build and push Docker image with Buildx (don't push on PR)
      # https://github.com/docker/build-push-action
      - name: Build and push Docker image
        id: build-and-push
        uses: docker/build-push-action@bcafcacb16a39f128d818304e6c9c0c18556b85f  # v7.1.0
        with:
          context: .
          build-args: |
            PG_VERSION=${{ steps.pgver.outputs.major }}
          push: ${{ startsWith(github.ref, 'refs/tags/v') && github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max


      # Sign the resulting Docker image digest except on PRs.
      # This will only write to the public Rekor transparency log when the Docker
      # repository is public to avoid leaking data.  If you would like to publish
      # transparency data even for private images, pass --force to cosign below.
      # https://github.com/sigstore/cosign
      - name: Sign the published Docker image
        if: ${{ startsWith(github.ref, 'refs/tags/v') && github.event_name != 'pull_request' }}
        # cosign v2 keyless signing uses the GHA OIDC token automatically
        # (no COSIGN_EXPERIMENTAL needed). --yes skips v2's interactive
        # confirmation prompt, which would otherwise hang in CI.
        env:
          TAGS: ${{ steps.meta.outputs.tags }}
          DIGEST: ${{ steps.build-and-push.outputs.digest }}
        run: echo "$TAGS" | xargs -I {} cosign sign --yes {}@"$DIGEST"

      # SLSA build provenance, attached to the pushed image as an OCI referrer.
      # The digest is identical across registries (same manifest content), so
      # the two steps differ only in subject-name. This attests an
      # already-pushed digest, so it's independent of buildx's in-build
      # provenance (unlike oracle, no wrapper-index concern here). Also recorded
      # in this repo's GitHub attestation store regardless of push-to-registry.
      - name: Attest provenance (Docker Hub)
        if: ${{ env.IS_RELEASE == 'true' }}
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY_DOCKERHUB }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.build-and-push.outputs.digest }}
          push-to-registry: true

      - name: Attest provenance (GHCR)
        if: ${{ env.IS_RELEASE == 'true' }}
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY_GHCR }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.build-and-push.outputs.digest }}
          push-to-registry: true

      # Software Bill of Materials: scan the pushed image and attest the SPDX
      # SBOM to each registry as an OCI referrer, mirroring the provenance steps
      # above. anchore/sbom-action (syft) scans the linux/amd64 image (the
      # runner's arch); both arches carry the same packages, so one SBOM is
      # attested against the multi-arch index digest.
      - name: Generate SBOM
        if: ${{ env.IS_RELEASE == 'true' }}
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610  # v0.24.0
        with:
          image: ${{ env.REGISTRY_DOCKERHUB }}/${{ env.IMAGE_NAME }}@${{ steps.build-and-push.outputs.digest }}
          format: spdx-json
          output-file: sbom.spdx.json

      # Uses the unified actions/attest (actions/attest-sbom is deprecated). The
      # SPDX document is the predicate; predicate-type is the SPDX in-toto type.
      - name: Attest SBOM (Docker Hub)
        if: ${{ env.IS_RELEASE == 'true' }}
        uses: actions/attest@v4
        with:
          subject-name: ${{ env.REGISTRY_DOCKERHUB }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.build-and-push.outputs.digest }}
          predicate-type: https://spdx.dev/Document
          predicate-path: sbom.spdx.json
          push-to-registry: true

      - name: Attest SBOM (GHCR)
        if: ${{ env.IS_RELEASE == 'true' }}
        uses: actions/attest@v4
        with:
          subject-name: ${{ env.REGISTRY_GHCR }}/${{ env.IMAGE_NAME }}
          subject-digest: ${{ steps.build-and-push.outputs.digest }}
          predicate-type: https://spdx.dev/Document
          predicate-path: sbom.spdx.json
          push-to-registry: true

      # Sync this repo's README to the Docker Hub repository's full description
      # on release (cosmetic; the image is already published above). The curated
      # short description is left untouched (no short-description input).
      # continue-on-error: a description-API failure (e.g. DOCKER_PASSWORD is a
      # PAT lacking description-write scope) must never fail a release that has
      # already pushed the image; check the first release's logs to confirm sync.
      - name: Sync README to Docker Hub description
        if: ${{ env.IS_RELEASE == 'true' }}
        continue-on-error: true
        uses: peter-evans/dockerhub-description@1b9a80c056b620d92cedb9d9b5a223409c68ddfa  # v5.0.0
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: ${{ github.repository }}
